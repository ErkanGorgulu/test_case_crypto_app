import { StyleSheet } from 'react-native';
import { Colors } from '../../utils/colors';

export const styles = StyleSheet.create({
    historyButton: {
        width: 56, height: 56, backgroundColor: Colors.alabaster, alignItems: 'center', justifyContent: 'center',
    },
    historyText: {
        fontFamily: 'Campton-Medium', fontSize: 10, fontWeight: '400',
    },
});
